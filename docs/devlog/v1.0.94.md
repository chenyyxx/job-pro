# v1.0.94 开发日志

> 发布日期：2026-05-20  
> 对应 PR：#3（confirm-submit）+ commit d53bf9a（CI release）

---

## 新增功能

### confirm-submit apply flow（PR #3，fatmmouse）

**文件：** `cli/src/index.ts`（主要改动，+266/-142 行）

在 `apply --really-submit` 流程中，提交前新增确认步骤：展示 staged payload 摘要，要求用户键入确认字符串后才执行实际 POST。防止误操作。

其余修改：
- `cli/src/agibot.ts`、`cli/src/netease.ts`、`cli/src/vivo.ts` — 小调整适配新流程
- `docs/auto-apply.md` — 更新投递流程文档
- `examples/walkthrough.md` — 补充确认步骤说明

### CI 自动发布（commit d53bf9a，LAWTED）

**文件：** `.github/workflows/release.yml`（新增），`cli/package.json`

- 新增 GitHub Actions workflow：push 到 main 且 `cli/package.json` 版本号变动时，自动 `npm publish`
- package name 改为 `@ha7ch/job-pro`（加 scope），与 npm 上已发布的包名对齐
- 版本从 1.0.93 → 1.0.94

---

## 已知问题

无新增已知问题。

---

## 路线图

见 [v1.0.93 devlog](./v1.0.93.md#5-预期目标--功能路线图)，本版本未更改路线图。
